[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.120.4",
    "grpcio>=1.76.0",
    "grpcio-tools>=1.76.0",
    "openai>=2.6.1",
    "pillow>=12.0.0",
]

[tool.setuptools]
packages = ["routes", "clients", "services", "generated"]