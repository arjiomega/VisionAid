services:
  tts-grpc:
    build:
      context: ./services/tts
      dockerfile: Dockerfile
    ports:
      - 50052:50052

  stt-grpc:
    build:
      context: ./services/stt
      dockerfile: Dockerfile
    ports:
      - 50051:50051

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - 8000:8000