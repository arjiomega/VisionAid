services:
  tts-grpc:
    build:
      context: ./services/tts
      dockerfile: Dockerfile

  stt-grpc:
    build:
      context: ./services/stt
      dockerfile: Dockerfile

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${BACKEND_PORT:-8000}:8000"