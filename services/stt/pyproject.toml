[project]
name = "stt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.11.0",
    "grpcio>=1.76.0",
    "grpcio-tools>=1.76.0",
    "torch>=2.9.1",
    "transformers==4.57.3",
]

[tool.setuptools]
packages = ["stt"]